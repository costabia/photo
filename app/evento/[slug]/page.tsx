import { notFound } from "next/navigation";
import { createClient, isSupabaseConfigured } from "@/lib/supabase-server";
import GuestCamera from "@/components/GuestCamera";
export const dynamic = 'force-dynamic';
export default async function EventPage({ params }: { params: Promise<{ slug:string }> }) { const { slug } = await params; if (!isSupabaseConfigured()) return <main className="page-shell"><div className="container" style={{maxWidth:680,flex:1,display:'flex',alignItems:'center',justifyContent:'center',textAlign:'center'}}><div><h1 style={{fontSize:32}}>Configure o Supabase</h1><p style={{color:'var(--muted)',lineHeight:1.7}}>Crie o arquivo <code>.env.local</code> com as variáveis do arquivo <code>.env.example</code>, reinicie o servidor e abra esta página novamente.</p></div></div></main>; const supabase=await createClient(); const {data:event}=await supabase.from('events').select('*').eq('slug',slug).single(); if(!event) notFound(); return <main className="page-shell"><div className="container" style={{maxWidth:680,flex:1,display:'flex',flexDirection:'column'}}><header style={{padding:'30px 0',textAlign:'center'}}><span className="eyebrow">O nosso casamento</span></header><section style={{flex:1,display:'flex',flexDirection:'column',justifyContent:'center',alignItems:'center',textAlign:'center',padding:'30px 0 70px'}}><h1 style={{fontSize:'clamp(42px,12vw,72px)',lineHeight:1.06,margin:'0 0 22px'}}>{event.name}</h1><p style={{fontSize:18,color:'var(--muted)',margin:'0 0 42px'}}>Registre esse dia pelos seus olhos.</p><GuestCamera eventId={event.id}/></section></div></main> }

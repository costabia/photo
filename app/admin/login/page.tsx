'use client';
import { useState } from 'react'; import { createClient } from '@/lib/supabase-browser';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error){const message=error.message.toLowerCase();setError(message.includes('email not confirmed')?'Confirme o e-mail do usuário no Supabase antes de entrar.':message.includes('invalid login credentials')?'E-mail ou senha incorretos.':`Não foi possível entrar: ${error.message}`);setLoading(false)}else window.location.assign('/admin')};return <main className="page-shell"><div className="container" style={{maxWidth:440,flex:1,display:'flex',alignItems:'center'}}><form className="card" style={{padding:32,width:'100%'}} onSubmit={submit}><span className="eyebrow">Área dos noivos</span><h1 style={{fontSize:42,margin:'12px 0 28px'}}>Bem-vindos</h1><label style={{display:'block',fontSize:13,marginBottom:7}}>E-mail</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} style={{marginBottom:18}}/><label style={{display:'block',fontSize:13,marginBottom:7}}>Senha</label><input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)} style={{marginBottom:20}}/>{error&&<p style={{color:'#a84b4b',fontSize:13}}>{error}</p>}<button className="btn btn-primary" style={{width:'100%'}} disabled={loading}>{loading?'Entrando…':'Entrar'}</button></form></div></main>}
